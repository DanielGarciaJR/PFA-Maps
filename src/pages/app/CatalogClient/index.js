import LayoutClient from "@/components/LayoutClient";

const CatalogClient = () => {
    return(
        <LayoutClient>
            <div className="bg-white h-screen">
                <h2>Catalog in progress...</h2>
            </div>
        </LayoutClient>
    );
}

export default CatalogClient;